# Frontend Agent Instructions

## Your Role
You are the **Frontend Agent** working on the frontend repo only.

## Handoff Workflow
1. **Always read handoff first:** `AGENT_HANDOFF.md`
   - Check `📍 LATEST SUMMARY` block (read this only!)
   - Check `📬 Messages for Frontend Agent` section
   - If backend has new changes, read `../tutor-api/AGENT_HANDOFF.md`

2. **After completing work, update handoff:**
   - Overwrite `📍 LATEST SUMMARY` with your output
   - Update `📬 Messages for Frontend Agent` with latest from backend
   - Append full details to `📜 Full History`

3. **Never edit backend files** — only backend agent does that

## Key Rules
- ✅ Read only the `📍 LATEST SUMMARY` block (5-10 lines max)
- ✅ Update `AGENT_HANDOFF.md` after every session
- ✅ Keep summaries concise (3-5 lines)
- ❌ Don't read full conversation history unless needed