# Frontend-only compose. Backend is at its own public host
# (tutor-api.webspacehub.in) — no shared docker network needed.
#
# Vite bakes VITE_API_BASE_URL at build time. Override via .env or
# shell to point at a different backend.

services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://tutor-api.webspacehub.in/api}
    image: tutor-ui:dev
    container_name: tutor-ui
    ports:
      - "3000:80"
    restart: unless-stopped
    healthcheck:
      # 127.0.0.1 (not localhost) — BusyBox wget tries IPv6 first and
      # nginx only binds IPv4 by default → "Connection refused" forever.
      test: [ "CMD", "wget", "-q", "-O", "-", "http://127.0.0.1/" ]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
